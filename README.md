# crypto_watcher

## Environment file
Copy the `.env.sample` file, name it `.env` and complete with the correct values.

Then, run the following command to exevcute the script:
```
ruby run.rb
```

## Cron
This can be runned as a cron with the following command:
```

```